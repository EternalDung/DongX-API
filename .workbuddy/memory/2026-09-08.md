# 2026-09-08 工作日志

## 模型调用统计：单一 primary_mode 误导修复（方案 C + 样式 B）
- 背景：用户今日先 RAG 用 deepseek、再 chat 用 deepseek，统计卡只显示一行 deepseek-v4-flash 且 `primary_mode=rag`，看起来「都是 rag」。实际是平票（rag×1, chat×1）SQL 取到 rag。数据合并本身正确（请求数=2、tokens=1.5K 均对），误导来自单 tag。
- 方案 C：按模型合并一行 + 把 mode 分布可视化；用户选样式 B（并排彩色 chip）。
- 改动文件：
  - `src-tauri/src/models/mod.rs`：`ModelStat.primary_mode: String` → `mode_breakdown: std::collections::HashMap<String, i64>`
  - `src-tauri/src/db/repository.rs`：`model_stats` 第二查询改 `SELECT model, mode, COUNT(*) AS cnt ... GROUP BY model, mode`，构造 `model -> {mode->count}`；`ModeCountRow` 加 `cnt: i64`
  - `src/types/index.ts`：`primary_mode: string` → `mode_breakdown: Record<string, number>`
  - `src/components/dashboard/ModelStatsTable.tsx`：渲染并排 chip（`modeChipVariant` + `sortedModes`）；排序先数据面 chat/responses/messages 后 RAG rag/wiki，组内按次数降序；颜色 rag=粉、wiki=黄、其余灰
- 验证：`cargo check`（隔离 target `/d/DongX/.tmpcheck`）0 error；`npm run build` 通过（仅预存 chunk 警告）
- 范围：只改 Dashboard，未动 LogsPage

## 关键约定/坑
- D: 双视图陷阱：本次仍用 python 原子读-改-写-断言（与 cargo/tsc 同视图），check/build 均绿。
- cargo 根目录在 `src-tauri`（不在 `D:\DongX` 顶层）。

## 设计图决策记录
- 给用户出过「模型调用统计徽章样式」三方案对比 mockup（A 合并文本 / B 并排 chip / C 逗号文本），用户选 B。
