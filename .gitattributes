# Pin SQL migration files to LF line endings.
# Prevents the "migration N was previously applied but has been modified"
# startup panic on Windows: a CRLF<->LF flip changes the sqlx SHA-384
# checksum recorded in _sqlx_migrations. (Fixed 2026-08-30.)
src-tauri/migrations/* text eol=lf
